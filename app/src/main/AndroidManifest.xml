<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user_.bookinguser">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.location.gps" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/loogoo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/carlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoAvtivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistarActivity" />
        <activity android:name=".Setting" />
        <activity android:name=".WelcomToMyCardActivity" />
        <activity android:name=".ForgetPssword" />
        <activity android:name=".SigInActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ListCars" />
        <activity android:name=".MapsActivityEdite"/>
        <activity android:name=".MyBooking" />
        <activity android:name=".BokkongStepOne"/>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".BookingStepTwo" />
    </application>

</manifest>